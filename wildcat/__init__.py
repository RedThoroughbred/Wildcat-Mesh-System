"""Wildcat Mesh — "The Den".

One installable package for everything that runs on the Pi: config, paths,
(later) the MQTT bus, meshd, the BBS, telemetry, and the Observatory.

Phase 1 ships ``wildcat.config`` + ``wildcat.paths`` + the ``wildcat`` CLI.
"""

__version__ = "2.0.0a1"
