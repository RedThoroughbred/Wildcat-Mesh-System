# /etc/systemd/journald.conf.d/wildcat.conf — keep the journal from eating the SD card.
# Installed by deploy/install.sh --journald. Restart journald after changing:
#   sudo systemctl restart systemd-journald
[Journal]
SystemMaxUse=200M
MaxRetentionSec=1month
