[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "wildcat-mesh"
version = "2.0.0a1"
description = "Wildcat Mesh — The Den: BBS, telemetry, observatory and (soon) meshd/brain for a Meshtastic LoRa mesh on a Raspberry Pi"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "GPL-3.0-only" }
dependencies = [
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
# Everything the three Pi services need. `pip install -e '.[den]'` on the Pi.
den = [
    "meshtastic==2.7.4",
    "pypubsub",
    "requests",
    "flask==3.0.0",
    "flask-socketio==5.3.5",
    "python-socketio==5.10.0",
    "apscheduler==3.10.4",
]
dev = ["pytest>=7"]

[project.scripts]
wildcat = "wildcat.cli:main"

[tool.setuptools]
packages = ["wildcat"]

[tool.pytest.ini_options]
testpaths = ["tests"]
